import { Request, Response, NextFunction } from 'express';

// TODO
const getStatisticsById = async (req: Request, res: Response, next: NextFunction) => {
    try {
    } catch (e) {
        next(e);
    }
};

export { getStatisticsById };
